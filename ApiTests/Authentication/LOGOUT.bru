meta {
  name: LOGOUT
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/logout
  body: none
  auth: inherit
}

script:post-response {
  bru.setVar('session', '');
  
}

settings {
  encodeUrl: true
  timeout: 0
}
