meta {
  name: PLAYLISTS - 1: CREATE ( 400 )
  type: http
  seq: 2
}

post {
  url: {{base_url}}/test/api/v1/collection/playlists
  body: multipartForm
  auth: inherit
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
