meta {
  name: PLAYLISTS - 1: CREATE ( 400[2] )
  type: http
  seq: 3
}

post {
  url: {{base_url}}/test/api/v1/collection/playlists
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  title: test playlist
  random: nonsense
}

settings {
  encodeUrl: true
  timeout: 0
}
