meta {
  name: ITEMS - 1: CREATE ( 400 [2] )
  type: http
  seq: 3
}

post {
  url: {{base_url}}/test/api/v1/collection/items
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  title: music title
  playlist: music-metal
  not-a-thing: this is
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
